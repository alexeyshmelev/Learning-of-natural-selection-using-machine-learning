num_classes = 100 # количество классов в адаче классификации
input_size = 100 # допустим, слово в предложении кодируется как one-hot вектор. Тогда размер этого вектора и есть input_size (в нашем случае, сколько классов - такой и input_size)
hidden_size = 10 # размер скрытого слоя (может быть любым)
batch_size = 1 # сколько классов (примеров) мы треируем за раз
sequence_length = 1 # допустим, наш input - это список из 10 цифр. Это можно предствить как предложение. Тогда, так как в предложении всего одно слово (этот список), то размер предложения равен 1
num_layers = 1 # количество обычных слоёв RNN (чем больше, тем сеть будет более глубокая)
boot_from_file = False # определяем класс с помощью уже натренированной сети (Да/Нет)
train_number = 10 # сколько примеров на каждый класс
data_loader_batch_size = input_size + 1 # указывает, как делить большой список
dim = 2 # округление при поиске файлов
